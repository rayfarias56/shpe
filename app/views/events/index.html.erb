<div class="container">
<h1>Listing events</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Location</th>
    <th>Start time</th>
    <th>End time</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @events.each do |event| %>
  <tr>
    <td><%= event.name %></td>
    <td><%= event.description %></td>
    <td><%= event.location %></td>
    <td><%= event.start_time %></td>
    <td><%= event.end_time %></td>
    <td><%= link_to 'Register', event_registration_path(id: event.id)%></td>
    <td><%= link_to 'Show', event %></td>
    <td><%= link_to 'Edit', edit_event_path(event) if eboard_user?%></td>
    <td><%= link_to 'Destroy', event, method: :delete, data: { confirm: 'Are you sure?' } if eboard_user? %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_event_path if eboard_user? %>


</div>